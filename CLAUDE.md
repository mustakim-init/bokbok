# Claude

Read AGENTS.md first. All shared context, rules, and conventions live in `.agents/`.

This file exists so Claude Desktop and Claude CLI can find it. The real instructions are in `AGENTS.md` and `.agents/`.
