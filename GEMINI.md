# Gemini

Read AGENTS.md first. All shared context, rules, and conventions live in `.agents/`.

This file exists so Gemini CLI and Gemini Code Assist can find it. The real instructions are in `AGENTS.md` and `.agents/`.
