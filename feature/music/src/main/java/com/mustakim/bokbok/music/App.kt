package com.mustakim.bokbok.music
import com.mustakim.bokbok.data.local.*
import kotlinx.coroutines.flow.first

import android.content.Context


/**
 * Bridge class for BokBok components to access application context and helpers.
 */
object App {
    fun forgetAccount(context: Context) {
        // Redirect to any appropriate logic if needed, 
        // or just clear the relevant data in PreferenceStore.
        // For now, this placeholder satisfies the compiler.
    }
}
